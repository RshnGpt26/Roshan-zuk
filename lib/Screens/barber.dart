import 'package:flutter/material.dart';

class barber extends StatelessWidget {
  const barber({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Container(
        child: Text('Roshan Fjhflutter'),
      ),
    );
  }
}
